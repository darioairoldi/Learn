---
title: "pe-meta per-type validation checklists"
description: "Per-type structural validation checklists with exemplary quality bar thresholds — enables self-contained pe-meta validation without delegating to pe-gra validators"
version: "1.0.2"
last_updated: "2026-05-21"
domain: "prompt-engineering"
goal: "Provide internalized type-specific structural validation checklists so pe-meta-validator does not need to delegate to pe-gra validators"
scope:
  covers:
    - "Structural validation checklist per artifact type (8 types)"
    - "Exemplary quality bar thresholds alongside standard bar"
    - "YAML field requirements per type"
    - "Type-specific boundary and reference rules"
  excludes:
    - "Strategic validation criteria (lives in 05.06)"
    - "Dimension execution logic (lives in pe-meta-validator)"
    - "Dimension catalog (lives in 05.07)"
boundaries:
  - "Checklists define WHAT to check, not HOW to check — execution logic is in the validator agent"
  - "Exemplary bar is for PE-for-PE artifacts; standard bar is for domain artifacts"
rationales:
  - "Internalized checklists eliminate pe-gra delegation — single-agent validation is simpler and applies the exemplary bar"
  - "Side-by-side standard/exemplary columns let the validator select the appropriate bar per artifact domain"
---

# pe-meta Per-Type Validation Checklists

## How to use

The pe-meta-validator loads this file and dispatches to the appropriate section based on the target artifact's file path:
- `.copilot/context/` → Context file checklist
- `.agent.md` → Agent checklist
- `.prompt.md` → Prompt checklist
- `.instructions.md` → Instruction file checklist
- `SKILL.md` → Skill checklist
- `.template.md` → Template checklist
- `.json` in `hooks/` → Hook checklist
- `prompt-snippets/` → Snippet checklist

---

## Context files

| Check | Standard bar | Exemplary bar (PE-for-PE) | Dimension |
|---|---|---|---|
| `title:` present | Required | Required | D1 |
| `description:` present | Required | Required | D1 |
| `version:` SemVer | Required | Required | D1 |
| `last_updated:` date | Required | Required | D1 |
| `goal:` single sentence | Recommended | Required | D1 |
| `scope.covers:` list | Recommended | Required | D1 |
| `scope.excludes:` list | Optional | Required | D1 |
| `boundaries:` list | Optional | Required (≥2) | D1 |
| `rationales:` list | Optional | Required (≥1) | D1 |
| All `📖` refs resolve | Required | Required | D2 |
| All markdown links resolve | Required | Required | D2 |
| Token count ≤2,500 (hard ceiling; prefer ≤1,000 when practical) | Required | Required | D3 |
| N-1 separation on rule sections | Recommended | Required | D14 |
| Naming: kebab-case with tier prefix | Required | Required | D14 |

---

## Agent files

| Check | Standard bar | Exemplary bar (PE-for-PE) | Dimension |
|---|---|---|---|
| `description:` present | Required | Required | D1 |
| `agent:` mode (plan/agent) | Required | Required | D1 |
| `tools:` list | Required | Required | D1 |
| `version:` SemVer | Recommended | Required | D1 |
| `goal:` single sentence | Recommended | Required | D1 |
| `scope.covers:` + `excludes:` | Recommended | Required | D1 |
| `boundaries:` list | Recommended | Required | D1 |
| `rationales:` list | Optional | Required (≥2) | D1 |
| `handoffs:` with `send: true` where needed | Recommended | Required | D1 |
| `context_dependencies:` declared | Recommended | Required | D1 |
| All `📖` refs resolve | Required | Required | D2 |
| All markdown links resolve | Required | Required | D2 |
| All slash-command refs resolve | Required | Required | D2 |
| Handoff targets resolve to existing agents | Required | Required | D2 |
| Token count ≤2,500 | Required | Required | D3 |
| `plan` mode → read-only tools only | Required | Required | D4 |
| `agent` mode → includes write tools | Required | Required | D4 |
| Tool count 3-7 | Required | Required | D4 |
| Boundaries: ≥3 Always / ≥1 Ask / ≥2 Never | Standard | Exemplary: ≥5/≥2/≥3 | D5 |
| Persona section present | Required | Required | D14 |
| Process phases defined | Required | Required | D14 |
| Test scenarios (≥3) | Required | Required | D14 |

---

## Prompt files

| Check | Standard bar | Exemplary bar (PE-for-PE) | Dimension |
|---|---|---|---|
| `name:` slash-command | Required | Required | D1 |
| `description:` present | Required | Required | D1 |
| `agent:` mode (plan/agent) | Required | Required | D1 |
| `model:` specified | Recommended | Required | D1 |
| `tools:` list | Required | Required | D1 |
| `version:` SemVer | Recommended | Required | D1 |
| `goal:` single sentence | Recommended | Required | D1 |
| `scope.covers:` + `excludes:` | Recommended | Required | D1 |
| `argument-hint:` usage example | Recommended | Required | D1 |
| `rationales:` list | Optional | Required (≥1) | D1 |
| All `📖` refs resolve | Required | Required | D2 |
| All slash-command refs resolve | Required | Required | D2 |
| Agent refs in `handoffs:` resolve | Required | Required | D2 |
| Token count ≤3,000 | Required | Required | D3 |
| `plan` mode → read-only tools only | Required | Required | D4 |
| `agent` mode → includes write tools | Required | Required | D4 |
| Tool count 3-7 | Required | Required | D4 |
| Test scenarios (≥3) | Required | Required | D14 |

---

## Instruction files

| Check | Standard bar | Exemplary bar (PE-for-PE) | Dimension |
|---|---|---|---|
| `description:` present | Required | Required | D1 |
| `applyTo:` glob pattern | Required | Required | D1 |
| `version:` SemVer | Recommended | Required | D1 |
| `goal:` single sentence | Recommended | Required | D1 |
| `rationales:` list | Optional | Required (≥1) | D1 |
| All `📖` refs resolve | Required | Required | D2 |
| Token count ≤1,500 | Required | Required | D3 |
| Rules are testable/mechanical only (R-S8) | Required | Required | D14 |
| No behavioral/strategic rules (R-S8) | Required | Required | D14 |
| Severity index present | Recommended | Required | D14 |

---

## Skill files (SKILL.md)

| Check | Standard bar | Exemplary bar (PE-for-PE) | Dimension |
|---|---|---|---|
| `name:` present | Required | Required | D1 |
| `description:` present (trigger phrases) | Required | Required | D1 |
| All `📖` refs resolve | Required | Required | D2 |
| Token count ≤2,500 | Required | Required | D3 |
| Progressive disclosure (3 levels) | Recommended | Required | D14 |

---

## Template files

| Check | Standard bar | Exemplary bar (PE-for-PE) | Dimension |
|---|---|---|---|
| Filename ends with `.template.md` | Required | Required | D1 |
| Placeholders documented | Recommended | Required | D1 |
| All `📖` refs resolve | Required | Required | D2 |
| Token count ≤2,500 | Required | Required | D3 |
| Schema sections present | Required | Required | D14 |

---

## Hook files (JSON)

| Check | Standard bar | Exemplary bar (PE-for-PE) | Dimension |
|---|---|---|---|
| Valid JSON | Required | Required | D1 |
| `event:` field present | Required | Required | D1 |
| `steps:` array present | Required | Required | D1 |
| All file paths in steps resolve | Required | Required | D2 |

---

## Prompt snippets

| Check | Standard bar | Exemplary bar (PE-for-PE) | Dimension |
|---|---|---|---|
| Filename follows naming convention | Required | Required | D1 |
| All `📖` refs resolve | Required | Required | D2 |
| Token count ≤1,000 | Required | Required | D3 |
| Reusable (no hardcoded paths) | Recommended | Required | D14 |

<!--
article_metadata:
  filename: "05.08-pe-meta-type-checklists.md"
  created: "2026-05-15"
  type: "context"
-->

