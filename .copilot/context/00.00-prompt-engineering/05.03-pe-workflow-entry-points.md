---
title: "PE workflow entry points"
description: "Decision guide for choosing the right prompt or agent — standalone vs orchestrator, validation, direct agent usage, meta-prompts"
version: "2.4.0"
last_updated: "2026-04-24"
domain: "prompt-engineering"
goal: "Guide users to choose the right prompt or agent when starting PE workflows"
rationales:
  - "Users need a decision tree to distinguish standalone vs orchestrator vs meta-prompts"
  - "Wrong entry point selection leads to wasted effort or incomplete results"
---

# PE Workflow Entry Points

**Purpose**: Decision guide for choosing the right prompt or agent when working with prompt engineering artifacts — differentiating standalone prompts from orchestrator prompts and clarifying when to use each.

**Referenced by**: All PE artifact creators, meta-review prompts, users starting PE workflows

---

## Quick Decision: What Do You Want to Do?

```
What do you want to do?
│
├─ CREATE a new artifact?
│  ├─ Complex/uncertain requirements → Orchestrator Prompt (Phase A)
│  └─ Clear requirements, known pattern → Standalone Prompt (Phase B)
│
├─ REVIEW/VALIDATE an existing artifact?
│  └─ → Validation Prompt (Phase C)
│
├─ UPDATE/FIX an existing artifact?
│  ├─ From validation report → Agent directly (Phase D)
│  └─ Ad hoc change → Standalone Prompt in update mode (Phase B)
│
├─ Review the PE SYSTEM itself?
│  └─ → Meta-Prompt (Phase E)
│
└─ Not sure which artifact type to create?
   └─ → Read 01.03-file-type-decision-guide.md first
```

---

## Phase A: Orchestrator Prompts (Complex Creation)

**Use when**: Requirements are unclear, scope is uncertain, or you want guided multi-phase creation with quality gates.

| Prompt | Slash Command | Creates | Agent Team | When to Use |
|---|---|---|---|---|
| `pe-prompt-design` | `/pe-prompt-design` | Prompt files + dependent agents | 8 agents (prompt + agent specialists) | New prompt with uncertain requirements, novel workflow, or when dependent agents may be needed |
| `pe-agent-design` | `/pe-agent-design` | Agent files | 4 agents (agent specialists) | New agent with role that needs challenge-testing, unclear tools, or possible decomposition |
| `pe-context-information-design` | `/pe-context-information-design` | Context files | 3 agents (context specialists) | New domain context with uncertain scope, possible overlap, or multi-file splitting needed |
| `pe-instruction-file-design` | `/pe-instruction-file-design` | Instruction files | 3 agents (instruction specialists) | New instruction file with unclear applyTo, possible conflicts |
| `pe-skill-design` | `/pe-skill-design` | Skills | 3 agents (skill specialists) | New skill with uncertain scope, multiple workflows |

**What orchestrators provide that standalone prompts do not:**
- Use case challenge validation (3–7 scenarios)
- Pattern research across existing codebase
- Quality gates between phases
- Automatic validation handoff
- Recursive creation of dependent agents

**Orchestrator workflow:**
```
User request → Researcher (requirements + patterns) → [User approval gate]
    → Builder (file creation) → Validator (quality check)
    → Updater (if issues) → Validator (re-check) → Done
```

---

## Phase B: Standalone Prompts (Direct Creation/Update)

**Use when**: Requirements are clear, you know the pattern, or you want a faster single-agent workflow.

| Prompt | Slash Command | Creates/Updates | When to Use |
|---|---|---|---|
| `pe-prompt-create-update` | `/pe-prompt-create-update` | Prompt files | Clear requirements, known template, quick creation or update |
| `pe-agent-create-update` | `/pe-agent-create-update` | Agent files | Clear role, known tools, quick creation or update |
| `pe-context-information-create-update` | `/pe-context-information-create-update` | Context files | New knowledge to document, source material available |
| `pe-instruction-file-create-update` | `/pe-instruction-file-create-update` | Instruction files | New file-type rules, known applyTo patterns |
| `pe-skill-create-update` | `/pe-skill-create-update` | Skills | Clear scope, known workflows, quick skill creation or update |

**Standalone vs Orchestrator — Decision Matrix:**

| Factor | → Standalone | → Orchestrator |
|---|---|---|
| Requirements clarity | Clear, specific | Vague, exploratory |
| Pattern familiarity | Known pattern, existing template | Novel workflow, no precedent |
| Scope confidence | Well-scoped, single artifact | May need decomposition |
| Tool requirements | Known tools, ≤7 | Unclear tools, possibly >7 |
| Time priority | Fast turnaround needed | Quality over speed |
| Dependent artifacts | None needed | May need supporting agents |

---

## Phase C: Validation Prompts (Review/Validate)

**Use when**: You have an existing artifact that needs quality verification.

| Prompt | Slash Command | Validates | Agent Team | When to Use |
|---|---|---|---|---|
| `pe-prompt-review` | `/pe-prompt-review` | Prompt files | `pe-prompt-validator` ↔ `pe-prompt-builder` | After creation, after updates, periodic review |
| `pe-agent-review` | `/pe-agent-review` | Agent files | `pe-agent-validator` ↔ `pe-agent-builder` | After creation, after updates, periodic review |
| `pe-skill-review` | `/pe-skill-review` | Skills | `pe-skill-validator` ↔ `pe-skill-builder` | After creation, after updates, periodic review |
| `pe-instruction-file-review` | `/pe-instruction-file-review` | Instruction files | `pe-instruction-validator` ↔ `pe-instruction-builder` | After creation, after updates, periodic review |
| `pe-context-information-review` | `/pe-context-information-review` | Context files | `pe-context-validator` ↔ `pe-context-builder` | After creation, after updates, periodic review |

**Validation workflow:**
```
Artifact → Tool alignment check (CRITICAL) → Structure check
    → Boundary check → Quality score → [If issues: builder → re-validate]
```

**Validation is mandatory after:**
- Any CRITICAL or HIGH impact change
- New artifact creation (usually auto-triggered via handoff)
- Changes to artifacts with 3+ dependents

---

## Phase D: Direct Agent Usage (Expert Mode)

**Use when**: You know exactly which agent to invoke and want to skip the orchestrator/prompt overhead.

| Agent | Best For |
|---|---|
| `@pe-prompt-researcher` | Quick requirements check, pattern search |
| `@pe-prompt-builder` | Build from spec, apply fixes |
| `@pe-prompt-validator` | Quick validation of a single file |
| `@pe-agent-researcher` / `@pe-agent-builder` / `@pe-agent-validator` | Same pattern for agents |
| `@pe-context-builder` | Create/update context files |
| `@pe-instruction-builder` | Create/update instruction files |
| `@pe-meta-validator` | Audit PE system (Ecosystem Audit mode) |
| `@pe-meta-optimizer` | Apply optimizations from audit report |

**When to use agents directly instead of prompts:**
- You have a clear, single-step task
- You already have the research report / specification
- You want to skip orchestration overhead
- You are an experienced user who knows the agent capabilities

---

## Phase E: Meta-Agents and Meta-Prompts (System Self-Improvement)

**Use when**: You want to review, optimize, or update the PE artifact system itself.

### Available Meta-Agents

| Agent | Purpose | Mode | Use Directly |
|---|---|---|---|
| `@pe-meta-validator` | Audit all PE artifacts for coherence, redundancy, completeness (Ecosystem Audit mode) | Read-only (`plan`) | `@pe-meta-validator` — produces audit report |
| `@pe-meta-optimizer` | Apply deduplication, token savings, structural improvements | Read-write (`agent`) | `@pe-meta-optimizer <audit-report>` — applies fixes |

**Meta-agent workflow:**
```
@pe-meta-validator (Ecosystem Audit) → audit report → @pe-meta-optimizer → changes → @pe-prompt-validator → re-validation
```

### Meta-Prompts (Slash Commands)

| Prompt | Purpose | Mode |
|---|---|---|
| `/pe-meta-prompt-engineering-update` | Incorporate new guidance (fullcheck) | Read-write |
| `/pe-meta-prompt-engineering-update healthcheck` | System review by scope+dimension | Read-only |
| `/pe-meta-prompt-engineering-update performancecheck` | Apply optimizations | Read-write |
| `/pe-meta-prompt-engineering-scheduled-review` | Weekly auto-detect stale areas | Read-write |
| `/pe-meta-prompt-engineering-release-monitor` | Track VS Code/Copilot releases | Read-write |

**Flags**: `--no-external` (local only), `--no-research` (skip researcher, requires file scope), `--incremental` (only changed files)

**Healthcheck parameters**: Scope (`all`/`context`/`agents`/...) + Dimensions (`coherence`/`structure`/`rules`/`references`/`budgets`)

### Scheduled Review Patterns

Three complementary mechanisms cover different triggers:

| Mechanism | Trigger | Scope | LLM needed |
|---|---|---|---|
| `pe-staleness-check` hook | Every session start | All context files | No (shell script) |
| `/pe-meta-prompt-engineering-scheduled-review` | Weekly (user-initiated) | Auto-detected stale areas | Yes |
| `/pe-meta-prompt-engineering-release-monitor` | New VS Code/Copilot release | Affected artifact types | Yes |

**Audit trail**: All reviews are recorded in [05.04-meta-review-log.md](05.04-meta-review-log.md).

---

## Common Scenarios

| Scenario | Entry Point |
|---|---|
| Simple prompt with clear requirements | `/pe-prompt-create-update` |
| Complex multi-agent workflow | `/pe-prompt-design` |
| Validate an existing artifact | `/pe-prompt-review` or `/pe-agent-review` |
| Fix a specific issue | `@{type}-builder` directly |
| Document new knowledge | `/pe-context-information-create-update` |
| New domain with uncertain scope | `/pe-context-information-design` |
| System-level audit | `@pe-meta-validator` |
| Weekly maintenance | `/pe-meta-prompt-engineering-scheduled-review` |
| New VS Code release | `/pe-meta-prompt-engineering-release-monitor` |

---

## Anti-Patterns

- **Orchestrator for simple tasks** — Use standalone prompts for known patterns
- **Standalone for novel workflows** — Use orchestrators for uncertain scope
- **Skipping validation** — Always validate after creation or update
- **Direct file editing** — Use builder agents for tracked, validated changes

---

## References

- **Internal**: [05.01-artifact-dependency-map.md](05.01-artifact-dependency-map.md), [05.02-artifact-lifecycle-management.md](05.02-artifact-lifecycle-management.md), [01.03-file-type-decision-guide.md](01.03-file-type-decision-guide.md)
- **Prompts**: `.github/prompts/00.00-prompt-engineering/` (all PE prompts)
- **Agents**: `.github/agents/00.00-prompt-engineering/` (all PE agents)

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 1.0.0 | 2026-03-08 | Initial version — decision trees, scenario mapping, anti-patterns | System |
| 1.1.0 | 2026-03-08 | Phase 3 — added meta-reviewer, pe-meta-optimizer to agent listing and Phase E; updated scenarios | System |
| 2.0.0 | 2026-03-10 | Consolidated meta-reviewer into pe-meta-validator (Mode 3: Ecosystem Audit). Updated all references. | System |
| 1.2.0 | 2026-03-08 | Phase 4 — meta-prompts now created; updated Phase E with slash commands and workflow | System |
| 2.1.0 | 2026-03-11 | Fixed ghost references: prompt-updater → pe-prompt-builder, agent-updater → pe-agent-builder (agents were merged in v2.0) | System |
| 2.2.0 | 2026-03-13 | Added Scheduled Review Patterns section — canonicalized hook + scheduled review + release monitor coordination. Added scheduled-review and release-monitor to Meta-Prompts table and Common Scenarios | System |
