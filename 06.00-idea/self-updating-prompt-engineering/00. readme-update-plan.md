---
title: "Update plan: self-updating prompt engineering vision"
author: "Dario Airoldi"
date: "2026-04-19"
categories: [prompt-engineering, planning]
description: "Phased plan for strengthening the vision and rationale document with explicit goals, structured rationales, and connections to existing PE infrastructure."
---

# Update plan: self-updating prompt engineering vision

## Table of contents

- [🎯 What this plan addresses](#-what-this-plan-addresses)
- [🔍 Assessment of current document](#-assessment-of-current-document)
- [💡 Proposed goal statement](#-proposed-goal-statement)
- [⚙️ Proposed rationale framework (R1–R14)](#️-proposed-rationale-framework-r1r14)
- [🏗️ Phased update plan](#️-phased-update-plan)
- [📋 New document structure](#-new-document-structure)
- [📚 References](#-references)

## 🎯 What this plan addresses

The [vision and rationale document](readme.md) is a strong conceptual piece. This plan transforms it from a persuasive narrative into a **governing document** — one precise enough that any downstream artifact (prompt, agent, context file) can trace its design back to a specific goal and a specific rationale.

Three structural weaknesses drive this update:

1. The **goal is implicit**, never explicitly stated as a measurable outcome
2. The **rationale is narrative**, not structured enough for downstream traceability
3. **Key technical enablers are absent** — capabilities the PE system already exploits but the document doesn't acknowledge

## 🔍 Assessment of current document

### What's strong (keep as-is or refine)

| Section | Strength |
|---------|----------|
| The problem | Clear three-force model (platform drift, internal drift, ecosystem evolution). Well-written, concrete. |
| The insight (opening) | The analogy to familiar maintenance work is effective. |
| The insight (two levels) | Architecture-level vs. quality-dimension-level analysis is a strong structural observation. |
| The insight (coupling) | Independent vs. coupled quality dimensions is a valuable design insight. |
| Design principles | Six solid principles. Need expansion, not replacement. |
| What it means / doesn't mean | Clear boundary-setting. Needs minor updates for Execute layer. |
| Risks and tensions | Comprehensive. Needs additions for new rationales. |
| Success criteria | Good foundation. Needs expansion and traceability to rationales. |

### What's weak (needs restructuring)

| Issue | Impact |
|-------|--------|
| No explicit goal statement | The document can't serve as the validation reference it claims to need. Readers infer the goal from behavior descriptions. |
| LLM capabilities listed as a bullet narrative | "They can search... They can analyze..." reads well but isn't referenceable. When a downstream artifact needs to justify a design choice, there's no rationale ID to point at. |
| Vision stops at "Propose" | The goal includes autonomous execution of low-risk changes, but the three-layer vision (Detect → Assess → Propose) has no Execute layer. |
| No connection to existing PE infrastructure | The document reads as entirely aspirational. It doesn't acknowledge that `meta-reviewer`, `meta-optimizer`, validation caching, dependency maps, and model-specific optimization already implement parts of this vision. |
| Definitions missing | "Reliable," "effective," "efficient," "autonomous" are used throughout but never defined in context. |

## 💡 Proposed goal statement

### Two dimensions

The goal has two dimensions that the current document conflates:

**Goal A — the self-update process itself:**

> Achieve maximally autonomous maintenance of prompt engineering artifacts — where the degree of autonomy is calibrated to the assessed risk of each change — through a self-update process that is itself reliable, effective, and efficient.

**Goal B — the artifacts the process produces:**

> Ensure that maintained artifacts reliably, effectively, and efficiently serve their defined purpose — producing predictable outputs (reliable), achieving their stated objectives (effective), and consuming minimal tokens, attention, and effort (efficient).

### Definitions

| Term | Definition in context |
|------|----------------------|
| **Reliable** | Produces consistent, predictable outcomes across invocations. A reliable self-update process detects the same drift when run twice. A reliable artifact produces the same quality output regardless of when it's invoked. |
| **Effective** | Achieves its stated purpose. An effective self-update process actually catches real drift and proposes correct fixes. An effective artifact actually guides the LLM to produce the desired output. |
| **Efficient** | Achieves its purpose with minimal resource consumption (tokens, time, human attention, cost). An efficient self-update process doesn't re-validate unchanged artifacts. An efficient artifact doesn't load unnecessary context. |
| **Autonomous** | Operates without human intervention for decisions at or below a validated risk threshold. The degree of autonomy is a gradient, not a binary — from "fully autonomous" (reference updates) to "fully human" (vision changes). |
| **Risk-calibrated** | The autonomy level for any action is determined by assessed impact × confidence. Low impact + high confidence = autonomous. High impact or low confidence = human review. |

### Combined goal statement

> Build a self-update system that maintains prompt engineering artifacts at peak reliability, effectiveness, and efficiency — with the maximum degree of autonomy that assessed risk allows — by continuously detecting drift, evaluating improvement opportunities, and executing validated corrections.

## ⚙️ Proposed rationale framework (R1–R14)

Each rationale is a specific technical capability or strategy that makes the goal achievable. Downstream artifacts can reference these by ID (e.g., "this design relies on R2 and R9").

### LLM capabilities

| ID | Rationale | Design implication |
|----|-----------|-------------------|
| **R1** | **LLM reasoning and judgment.** LLMs can analyze artifacts, compare implementations against specifications, identify inconsistencies, evaluate external work against internal standards, and make judgment calls about whether a change improves adherence to the vision. | Delegate analysis, comparison, and decision-making to LLMs. Reserve human oversight for decisions above the risk threshold. |
| **R2** | **Self-correction through multi-pass validation.** A single LLM pass can introduce errors. A second pass — by the same or a different model — can detect and correct those errors before they're committed. The validator ↔ updater cycle already demonstrates this. | No change is committed without at least one independent validation pass. Critical changes get multiple passes. |
| **R3** | **Model specialization.** Different models have different strengths. Reasoning models excel at analysis and planning. Standard models excel at execution and structured output. Small models suffice for deterministic-like tasks. | Assign models by task type. Research/analysis → reasoning models. Implementation → standard models. Simple validation → cheapest capable model. |
| **R7** | **External knowledge access.** LLMs can consume platform documentation, release notes, changelogs, community repositories, and best practices. This gives the system awareness of changes without requiring a human to read and summarize them first. | Monitor defined sources (VS Code release notes, Copilot documentation, trusted ecosystem projects) and feed relevant changes into the detect phase. |

### Processing strategies

| ID | Rationale | Design implication |
|----|-----------|-------------------|
| **R4** | **Deterministic processing for predictable operations.** Many maintenance tasks don't need LLM judgment: parsing YAML, checking file existence, comparing timestamps, counting dependencies, validating JSON schemas, computing content hashes. Deterministic tools are both cheaper and more reliable. | Use deterministic tools for all predictable operations. Reserve LLM processing for tasks requiring reasoning, judgment, or natural language understanding. |
| **R6** | **Decomposition for context window control.** LLM accuracy degrades as context grows (context rot). Self-update tasks spanning dozens of artifacts must be decomposed into focused units that each fit within an effective context window. | Self-update workflows decompose into phases (detect → assess → plan → validate → implement → verify). Each phase has a token budget. Handoffs carry only essential state. |
| **R8** | **Structured contracts between phases.** When LLM output feeds a downstream phase, structured formats (JSON, YAML, tables with defined schemas) bridge probabilistic LLM output with deterministic downstream processing. | Inter-phase communication uses defined schemas. LLM output is parsed deterministically into structured records that drive subsequent processing. |

### System infrastructure

| ID | Rationale | Design implication |
|----|-----------|-------------------|
| **R5** | **Metadata-driven automation.** Machine-readable metadata on artifacts — validation timestamps, content hashes, feature dependencies, dependency declarations, trust classifications — transforms implicit knowledge into actionable state. | Every artifact carries metadata declaring its dependencies, freshness requirements, and validation history. The self-update system reads this metadata to scope its work. |
| **R9** | **Dependency graph for scoped impact analysis.** The artifact dependency map enables precise impact analysis: given a change to artifact X, identify all artifacts that reference X, assess the blast radius, and scope validation to only what's affected. | Every self-update action starts with a dependency query. Changes to high-dependency artifacts get broader validation. Changes to leaf artifacts get minimal validation. |
| **R10** | **Validation caching to avoid redundant work.** The 7-day caching pattern prevents re-validating unchanged artifacts. Extended to self-updating: if an artifact was validated against the current platform version and hasn't changed since, skip it. | Self-update cycles check cached validation state before initiating processing. Only stale or changed artifacts enter the pipeline. |
| **R12** | **Artifact role and purpose declaration.** Each artifact carries a declaration of its role (what it does), its purpose (why it exists), and its success criteria (how to tell if it's working). This allows evaluation of whether an artifact is "still effective" — not just "still internally consistent." | Artifacts declare their purpose in a machine-readable way. The self-update system evaluates artifacts against their declared purpose, not just structural rules. |

### Governance model

| ID | Rationale | Design implication |
|----|-----------|-------------------|
| **R11** | **Risk-calibrated autonomy gradient.** Autonomy isn't binary. The system operates at different levels depending on assessed risk: autonomous (reference updates), autonomous with notification (redundancy removal), human approval (restructuring, adopting externals), human-only (vision changes). | Each proposed change is classified on this gradient. The gradient is configurable — humans can tighten or loosen based on observed performance. |
| **R13** | **Trust-calibrated ecosystem adoption.** The system evaluates external work against internal standards and either adopts it (replacing owned artifacts) or incorporates its ideas (improving owned artifacts) — calibrated to source trustworthiness. | Maintain a trust registry of sources. External artifacts from trusted sources trigger automatic evaluation. Adoption decisions surface to humans; incorporation can proceed autonomously if validated. |
| **R14** | **Progressive learning from outcomes.** The system tracks which changes improved quality and which introduced regressions. Over time, this creates a feedback signal: which types of autonomous changes succeed reliably, which need human review, which risk thresholds should be adjusted. | Log autonomous actions and their outcomes. Periodic meta-analysis informs threshold tuning — tightening where regressions occurred, loosening where the system performed reliably. |

## 🏗️ Phased update plan

### Phase 1: Add explicit goal section ✅

**Status:** Complete — applied to [readme.md](readme.md) on 2026-04-19.

**What was done:**
- Inserted `## 💡 The goal` section between "The problem" and "The rationale"
- Added combined goal statement (Goal A + Goal B)
- Added four-level autonomy gradient table
- Added key definitions table (reliable, effective, efficient, autonomous, risk-calibrated)
- Updated TOC to reflect new section and renamed "The insight" → "The rationale"

### Phase 2: Restructure "The rationale" — keep process, add enablers, connect them ✅

**Status:** Complete — applied to [readme.md](readme.md) on 2026-04-19.

**What was done:**
- Replaced the "They can search... They can analyze..." bullet narrative with a nine-step process workflow table (step, what happens, LLM role)
- Added the R1–R14 rationale framework as a "Technical enablers" subsection, grouped by category (LLM capabilities, processing strategies, system infrastructure, governance model)
- Added a process-to-rationale mapping table connecting each workflow step to its primary rationales
- Preserved the opening paragraph, two-level analysis (architecture vs. quality dimensions), coupling observation, and two prerequisites
- Trimmed redundant closing paragraphs (points moved earlier into the restructured flow)
- Renamed the "Two operating levels" content into a `### Two operating levels` subsection and added a `### Two prerequisites` subsection

### Phase 3: Strengthen "The vision" section ✅

**Status:** Complete — applied to [readme.md](readme.md) on 2026-04-19.

**What was done:**
- Added rationale references to each vision layer (Detect → R5, R7, R10; Assess → R1, R9, R13; Propose → R2, R8, R11)
- Added **Execute** as a fourth vision layer, relying on R2 (validation before commit), R4 (deterministic tools), R6 (decomposed implementation), R11 (autonomy gradient)
- Updated the vision opening paragraph to include "executes validated corrections" and the four-layer framing
- Updated the closing paragraph from "three layers" to "four layers"

### Phase 4: Refine design principles ✅

**Status:** Complete — applied to [readme.md](readme.md) on 2026-04-19.

**What was done:**
- Strengthened "Human governance, autonomous execution" — replaced binary "low risk = auto, high risk = human" with the four-level autonomy gradient table, added R2/R11 rationale references
- Added "Deterministic where possible" principle (R4, R8) — covers when to use deterministic tools vs. LLM processing across all four vision layers
- Added "Metadata-driven" principle (R5, R12) — covers how metadata enables scoping, caching, effectiveness evaluation, and trust classification
- Added rationale references (`*Relies on:*` lines) to existing principles where applicable (Graceful degradation → R10)

### Phase 5: Update success criteria ✅

**Status:** Complete — applied to [readme.md](readme.md) on 2026-04-19.

**What was done:**
- Added rationale traceability (`*Enabled by:*` lines) to all six existing success criteria
- Added criterion 7: "Self-update process cost stays bounded" (R4, R6, R10)
- Added criterion 8: "Autonomous execution success rate exceeds threshold" (R2, R11, R14)
- Removed implementation-specific reference ("as measured by the existing meta-review scoring") from criterion 6

### Phase 6: Add existing PE infrastructure references — DROPPED

**Status:** Dropped — the vision document should be implementation-independent. Specific artifact references (`meta-reviewer`, `16-artifact-dependency-map.md`, etc.) couple the vision to the current implementation, creating the same brittleness the vision is designed to prevent. Implementation mapping belongs in a separate artifact. The `### Existing infrastructure` subsection added in Phase 3 was also removed from `readme.md` for the same reason.

### Phase 7: Update risks, boundaries, and final review ✅

**Status:** Complete — applied to [readme.md](readme.md) on 2026-04-19.

**What was done:**
- **Risks and tensions:** Added four new risks for rationales introduced by the update:
  - Model specialization complexity (R3)
  - Deterministic rigidity (R4)
  - Metadata maintenance burden (R5, R12)
  - Insufficient outcome data (R14)
- **What it means / doesn't mean:** Added three entries for the Execute layer and autonomy gradient:
  - "Executing medium-risk changes autonomously with notification" (means)
  - "Learning from outcomes to tune autonomy thresholds" (means)
  - "Executing changes that haven't passed at least one validation pass" (doesn't mean)
- **References:** Removed three internal implementation-specific references (context engineering principles, artifact dependency map, PE improvement plan) to keep the vision implementation-independent. Added two external references (Lost in the Middle research, Diátaxis framework) that support specific rationales.
- **TOC:** Already correct from Phase 1 — no changes needed.

## 📋 New document structure

After all phases, the document structure becomes:

```
# Self-updating prompt engineering: vision and rationale

## 🎯 The problem                    ← keep (minor polish only)
## 💡 The goal                       ← NEW (Goal A + B, definitions)
## ⚙️ The rationale                  ← RESTRUCTURED from "The insight"
   ### LLM capabilities (R1, R2, R3, R7)
   ### Processing strategies (R4, R6, R8)
   ### System infrastructure (R5, R9, R10, R12)
   ### Governance model (R11, R13, R14)
   ### Quality dimensions and coupling  ← kept from current "The insight"
## 🏗️ The vision                     ← EXPANDED
   ### Detect                        ← + rationale connections
   ### Assess                        ← + rationale connections
   ### Propose                       ← + rationale connections
   ### Execute                       ← NEW
## 📋 Design principles              ← EXPANDED (+2 principles, gradient)
## 🔍 What it means / doesn't mean   ← UPDATED for Execute + gradient
## ⚠️ Risks and tensions             ← EXPANDED (+4 risks)
## ✅ Success criteria                ← EXPANDED (+2 criteria, traceability)
## 📚 References                     ← EXPANDED (internal PE refs)
```

**Estimated total document length:** ~3,500–4,000 words (current: ~3,100 words). The increase is modest because restructuring replaces narrative with structured content that's denser per word.

## 📚 References

- **[Self-updating PE vision](readme.md)** (internal)
  The document this plan updates.

- **[PE artifact analysis and improvement plan](../../99.00-temp/pe-context-files-improvement-plan.md)** (internal)
  Current-state analysis that identified gaps in the PE system — several of which connect to rationales R5, R9, R10, R12.

- **[Artifact dependency map](../../.copilot/context/00.00-prompt-engineering/16-artifact-dependency-map.md)** (internal)
  The dependency graph that enables R9 and informs scoped impact analysis.

- **[Context window and token optimization](../../.copilot/context/00.00-prompt-engineering/06-context-window-and-token-optimization.md)** (internal)
  Existing strategies for R4 (deterministic processing) and R6 (decomposition).

- **[Model-specific optimization](../../.copilot/context/00.00-prompt-engineering/09-model-specific-optimization.md)** (internal)
  Existing guidance for R3 (model specialization).

<!--
article_metadata:
  filename: "readme-update-plan.md"
  created: "2026-04-19"
  type: "plan"
  status: "draft"
-->
