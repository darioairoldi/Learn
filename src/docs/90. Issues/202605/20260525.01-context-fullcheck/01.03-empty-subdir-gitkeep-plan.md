---
title: "Plan 01.03 — D22 system-level: add .gitkeep to empty placeholder subdirectories"
author: "Dario Airoldi"
date: "2026-05-25"
version: "1.1.0"
status: "executed"
domain: "prompt-engineering"
categories: [prompt-engineering, pe-meta, context-organization]
description: "Plan to make the two empty subdirectories under `.copilot/context/00.00-prompt-engineering/` (`dependency-map/` and `structure/`) explicit placeholders for in-flight work by adding a `.gitkeep` file in each with a one-line comment explaining intended future use."
goal: "Clear the D22 system-level finding by making the two empty subdirectories explicit, tracked placeholders rather than implicit noise."
scope:
  covers:
    - ".copilot/context/00.00-prompt-engineering/dependency-map/.gitkeep (new)"
    - ".copilot/context/00.00-prompt-engineering/structure/.gitkeep (new)"
  excludes:
    - "Populating either directory with real content (separate future work)"
    - "Removing either directory (user confirmed in-flight)"
    - "Adding .gitkeep files anywhere else in the repository"
rationales:
  - "Empty directories produce D22 false-positive noise in --dim context-full reviews"
  - "User confirms both directories are placeholders for in-flight work, not abandonments"
  - ".gitkeep with an inline comment captures intent and silences future review noise"
  - "Trivial change with high signal-to-effort ratio"
---

# Plan 01.03 — D22 system-level: add .gitkeep to placeholder subdirectories

## 🎯 Goal

Make the two empty subdirectories under [.copilot/context/00.00-prompt-engineering/](../../../../../.copilot/context/00.00-prompt-engineering) explicit placeholders by adding a `.gitkeep` file in each, with a leading comment line describing the intended future content. This clears the D22 finding without removing slots that the user has confirmed are in-flight.

## 🔑 Target placeholders

| Path | Intended future content (user confirmation) |
|---|---|
| `.copilot/context/00.00-prompt-engineering/dependency-map/` | Reserved for future PE artifact dependency-map data (cross-artifact reference graph) |
| `.copilot/context/00.00-prompt-engineering/structure/` | Reserved for future PE artifact structural model data |

## 🔑 `.gitkeep` content template

Each `.gitkeep` file MUST contain a single comment line (`#` prefix is conventional in `.gitkeep` files, though the format is informal). Example body:

```
# Placeholder — reserved for future <purpose>. See src/docs/90. Issues/202605/20260525.01-context-fullcheck/overview.md (Plan 01.03).
```

Per-directory wording:

| Path | Body line |
|---|---|
| `dependency-map/.gitkeep` | `# Placeholder — reserved for PE artifact dependency-map data. See src/docs/90. Issues/202605/20260525.01-context-fullcheck/overview.md (Plan 01.03).` |
| `structure/.gitkeep` | `# Placeholder — reserved for PE artifact structural model data. See src/docs/90. Issues/202605/20260525.01-context-fullcheck/overview.md (Plan 01.03).` |

## 📋 Table of contents

- [Things to do](#-things-to-do)
- [Pre-execution validation](#-pre-execution-validation)
- [Step-by-step change list](#-step-by-step-change-list)
- [Acceptance criteria](#-acceptance-criteria)
- [Out of scope](#-out-of-scope)

## 📋 Things to do (✅ done)

### Step 1 — Confirm both directories are still empty (✅ done)

To do: verify nothing has been added to either directory since the audit.

- Run:
  ```powershell
  Get-ChildItem .copilot/context/00.00-prompt-engineering/dependency-map -Force
  Get-ChildItem .copilot/context/00.00-prompt-engineering/structure -Force
  ```
- Both MUST return zero items. If either is non-empty, stop and re-plan: the cleanup needs adjustment (perhaps the directory was already populated and no `.gitkeep` is needed).

### Step 2 — Create `dependency-map/.gitkeep` (✅ done)

To do: create [.copilot/context/00.00-prompt-engineering/dependency-map/.gitkeep](../../../../../.copilot/context/00.00-prompt-engineering/dependency-map/) with the body specified in the per-directory wording table.

### Step 3 — Create `structure/.gitkeep` (✅ done)

To do: create [.copilot/context/00.00-prompt-engineering/structure/.gitkeep](../../../../../.copilot/context/00.00-prompt-engineering/structure/) with the body specified in the per-directory wording table.

### Step 4 — Verify the files are tracked (✅ done)

To do: confirm both `.gitkeep` files are visible to git (not gitignored).

- Run `git ls-files .copilot/context/00.00-prompt-engineering/dependency-map .copilot/context/00.00-prompt-engineering/structure`.
- Expected output: two entries — `…/dependency-map/.gitkeep` and `…/structure/.gitkeep`. (They will appear after the next `git add`, so the verification is "would these files be added by `git add -A`?" — confirm via `git status`.)
- If `.gitignore` excludes either file, propose an exception line; do NOT bypass with `--force` silently.

## 🔎 Pre-execution validation

Before starting Step 2, confirm:

1. Both target paths exist as empty directories (Step 1 check).
2. No `.gitignore` pattern would exclude `.gitkeep` files anywhere under `.copilot/`.
3. The reference path used in the placeholder comment (`src/docs/90. Issues/202605/20260525.01-context-fullcheck/overview.md`) exists in the workspace.

If ANY check fails: stop and re-plan that specific step.

## 🔎 Step-by-step change list

| Step | File | Change | Type |
|---|---|---|---|
| 2 | `.copilot/context/00.00-prompt-engineering/dependency-map/.gitkeep` | Create with per-directory wording | New file |
| 3 | `.copilot/context/00.00-prompt-engineering/structure/.gitkeep` | Create with per-directory wording | New file |

## ✅ Acceptance criteria

- Both `.gitkeep` files exist on disk with the exact per-directory wording. (✅ done)
- Both files would be tracked by git (`git status` shows them as new untracked files; not gitignored). (✅ done)
- A re-run of `/pe-meta-context-review .copilot/context/00.00-prompt-engineering/ --dim context-full` reports zero empty subdirectories under D22. (🟡 todo — deferred to next review cycle)

## 🚫 Out of scope

- Populating either directory with real content — that is separate future work.
- Removing either directory — user confirmed in-flight.
- Adding `.gitkeep` files anywhere else in the repository.
- Modifying `.gitignore`.
