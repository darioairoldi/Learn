---
title: "Plan 01.02 — D17 cross-coherence: rename dim-group identifiers in pe-meta-context-review prompt"
author: "Dario Airoldi"
date: "2026-05-25"
version: "1.1.0"
status: "executed"
domain: "prompt-engineering"
categories: [prompt-engineering, pe-meta, prompt-review, contract-drift]
description: "Plan to align the `pe-meta-context-review.prompt.md` slash-command with the authoritative dimension catalog by renaming five occurrences of `context-quality-lifecycle` / `context-quality-health` to the canonical `context-full` / `context-health` group names defined in 05.07-pe-meta-dimension-catalog.md."
goal: "Eliminate the D17 cross-coherence drift between pe-meta-context-review.prompt.md and 05.07-pe-meta-dimension-catalog.md by renaming the prompt's dim-group identifiers to the canonical catalog names."
scope:
  covers:
    - ".github/prompts/00.09-pe-meta/pe-meta-context-review.prompt.md"
  excludes:
    - "Catalog 05.07 content (single source of truth, untouched)"
    - "Other pe-meta prompts (audited only — not edited as part of this plan)"
    - "Vision v13 (no preset-or-group references to update)"
rationales:
  - "The catalog explicitly states (boundary): 'Dimension group names are a contract — consumers reference them via --dim'"
  - "Five prompt occurrences vs. two catalog rows — fewer edits and lower risk in renaming the prompt"
  - "Other pe-meta prompts already use context-full / context-health; this fix harmonizes the family"
  - "Without the fix, --dim context-quality-* has undefined runtime behavior (either rejected or silently mis-routed)"
---

# Plan 01.02 — D17 cross-coherence: rename dim-group identifiers in prompt

## 🎯 Goal

Replace every occurrence of the undefined dim-group identifiers `context-quality-lifecycle` and `context-quality-health` in [pe-meta-context-review.prompt.md](../../../../../.github/prompts/00.09-pe-meta/pe-meta-context-review.prompt.md) with the canonical names defined in [05.07-pe-meta-dimension-catalog.md](../../../../../.copilot/context/00.00-prompt-engineering/05.07-pe-meta-dimension-catalog.md): `context-full` and `context-health` respectively.

## 🔑 Rename map (single source of truth)

| Prompt line (approx.) | Current text | Replacement |
|---|---|---|
| 28 | `--dim context-quality-lifecycle is selected` | `--dim context-full is selected` |
| 41 | `Lifecycle mode: --dim context-quality-lifecycle` | `Lifecycle mode: --dim context-full` |
| 42 | `Lifecycle health mode: --dim context-quality-health` | `Lifecycle health mode: --dim context-health` |
| 52 | `When --dim context-quality-lifecycle is selected, follow strict stage order:` | `When --dim context-full is selected, follow strict stage order:` |
| 71 | `Lifecycle health mode (--dim context-quality-health) runs lightweight checks…` | `Lifecycle health mode (--dim context-health) runs lightweight checks…` |

Five occurrences total. Three reference `context-quality-lifecycle` → `context-full`. Two reference `context-quality-health` → `context-health`.

**Catalog boundary preserved.** [05.07-pe-meta-dimension-catalog.md](../../../../../.copilot/context/00.00-prompt-engineering/05.07-pe-meta-dimension-catalog.md) is NOT edited by this plan — it is the authoritative source of dim-group names.

## 📋 Table of contents

- [Things to do](#-things-to-do)
- [Pre-execution validation](#-pre-execution-validation)
- [Step-by-step change list](#-step-by-step-change-list)
- [Acceptance criteria](#-acceptance-criteria)
- [Out of scope](#-out-of-scope)

## 📋 Things to do (✅ done)

### Step 1 — Re-confirm occurrences (✅ done)

To do: search the prompt for the two old identifiers to confirm the rename map is still complete and locations have not shifted.

- Run `Select-String -Path .github/prompts/00.09-pe-meta/pe-meta-context-review.prompt.md -Pattern 'context-quality-(lifecycle|health)'`.
- Confirm 5 matches at approximately the lines listed in the rename map.
- If matches differ in count or location: update the rename map BEFORE proceeding.

### Step 2 — Apply the five renames (✅ done)

To do: edit the prompt file to replace each occurrence. Prefer a multi-edit pass that applies all five substitutions in one operation to avoid intermediate inconsistent state.

- Use the exact substitutions from the rename map.
- Preserve all surrounding markup (formatting, code-fence boundaries, punctuation).
- Do NOT rename anything outside the listed occurrences (no preemptive renames in other prompts).

### Step 3 — Update YAML frontmatter (✅ done)

To do: bump the prompt's version and refresh `last_updated` to reflect the alignment.

- Increment the patch version (1.1.2 → 1.1.3).
- Set `last_updated: "2026-05-25"`.
- Add a one-line changelog entry (if a changelog section exists in the file) — otherwise skip (the version bump is the marker).

### Step 4 — Verify the rename is complete (✅ done)

To do: confirm zero remaining occurrences of the old identifiers anywhere in the prompts tree.

- Run `Select-String -Path .github/prompts -Pattern 'context-quality-(lifecycle|health)' -Recurse`.
- Expected output: **0 matches**.
- If any match remains outside the issue/plan files: investigate and patch in the same edit batch.

### Step 5 — Audit sibling prompts (✅ done, read-only)

To do: confirm no sibling pe-meta prompt has the inverse drift problem (using canonical names while expecting different behavior). This is an audit, not an edit.

- Open [pe-meta-review.prompt.md](../../../../../.github/prompts/00.09-pe-meta/pe-meta-review.prompt.md) and confirm it uses `context-full` / `context-health` consistently.
- Open [pe-meta-update.prompt.md](../../../../../.github/prompts/00.09-pe-meta/pe-meta-update.prompt.md) and confirm the same.
- If either sibling exhibits drift, raise a follow-up note in the overview's "Follow-up actions" table — do NOT edit sibling prompts as part of this plan.

### Step 6 — Verify the prompt is parseable (✅ done)

To do: ensure the rename did not break YAML or Markdown structure.

- Confirm YAML frontmatter still parses (open in VS Code; verify no validation errors).
- Confirm Markdown headings, code fences, and tables render correctly (preview the file).
- Confirm any anchors that referenced `context-quality-*` (if any embedded in markdown links inside the prompt) have been updated.

## 🔎 Pre-execution validation

Before starting Step 2, confirm:

1. The prompt file has not been modified since the audit (compare `git log -1`).
2. The catalog file [05.07-pe-meta-dimension-catalog.md](../../../../../.copilot/context/00.00-prompt-engineering/05.07-pe-meta-dimension-catalog.md) still defines `context-full` and `context-health` (Dimension groups section) — read-and-confirm.
3. No alternative dim-group name (e.g., `lifecycle` or `health` shortcuts) has been added to the catalog since the audit; if so, re-evaluate whether the rename target is correct.
4. The five occurrences from Step 1 match the rename map exactly.

If ANY check fails: stop and re-plan that specific step.

## 🔎 Step-by-step change list

| Step | File | Change | Type |
|---|---|---|---|
| 2 | `.github/prompts/00.09-pe-meta/pe-meta-context-review.prompt.md` | Five substring renames | Edit |
| 3 | `.github/prompts/00.09-pe-meta/pe-meta-context-review.prompt.md` | YAML version + `last_updated` bump | Edit |

(Step 5 is read-only; produces a follow-up note in the overview if drift is found.)

## ✅ Acceptance criteria

- `Select-String -Path .github/prompts -Pattern 'context-quality-(lifecycle|health)' -Recurse` returns **0 matches**. (✅ done — verified 2026-05-25, repo-wide grep returns no matches)
- The five locations from the rename map now use canonical group names. (✅ done — verified at prompt lines 28, 41, 42, 52, 71)
- The prompt's YAML frontmatter `version` is bumped and `last_updated` is `2026-05-25`. (✅ done — `version: "1.1.3"`, `last_updated: "2026-05-25"`)
- The prompt still parses (no YAML errors, Markdown structure intact). (✅ done — file re-read, frontmatter and headings intact)
- Sibling audit of [pe-meta-review.prompt.md](../../../../../.github/prompts/00.09-pe-meta/pe-meta-review.prompt.md) and [pe-meta-update.prompt.md](../../../../../.github/prompts/00.09-pe-meta/pe-meta-update.prompt.md) is recorded (pass or follow-up). (✅ done — pe-meta-review has no references; pe-meta-update uses canonical names at line 65; no drift, no follow-up needed)
- A re-run of `/pe-meta-context-review .copilot/context/00.00-prompt-engineering/ --dim context-full` clears the D17 finding for this prompt. (🟡 deferred — requires interactive slash-command execution; static verification above is sufficient to close)

## 🚫 Out of scope

- Editing the catalog ([05.07-pe-meta-dimension-catalog.md](../../../../../.copilot/context/00.00-prompt-engineering/05.07-pe-meta-dimension-catalog.md)) — single source of truth, untouched.
- Renaming any other dim-group identifier (e.g., `full`, `quality`, `optimize`) — not in scope.
- Updating other prompts proactively — Step 5 is an audit only; corrections trigger a follow-up plan.
- Vision v13 edits — no preset/group references in the vision require updating for this drift.
