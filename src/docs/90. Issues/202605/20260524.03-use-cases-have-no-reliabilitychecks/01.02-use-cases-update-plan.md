---
title: "Use-cases update plan — add 05-reliability folder with R1–R12 coverage"
author: "Dario Airoldi"
date: "2026-05-24"
categories: [issue, prompt-engineering, pe-meta, use-cases]
description: "Plan for scaffolding the new 05-reliability/ use-case folder, populating it with P0/P1/P2 use cases addressing every reliability anchor in the vision, and updating the catalog README and JSON index."
status: "completed"
version: "1.0.0"
parent: "overview.md"
workstream: "2"
prerequisites: "01.01 vision update (goal-trio mapping + robustness→adherence rename recorded)"
---

# Use-cases update plan — add `05-reliability/` folder

## 🎯 Objective

Extend the use-case catalog at [`20260503.02-vision-pe-meta-usecases/`](../../../../06.00-idea/self-updating-prompt-engineering/20260503.02-vision-pe-meta-usecases/) to include a fifth folder dedicated to **reliability** of the self-update process and of the artifacts it maintains. Every reliability anchor R1–R12 from the [issue overview](overview.md) MUST be addressed by at least one use case in the new folder.

## 📋 Prerequisites

- Workstream 1 ([vision update](01.01-visionupdate-plan.md)) completed — vision states the goal-trio↔folder mapping and records the `robustness`→`adherence` rename so this plan can rename the existing `--dim` value safely.

## 🔎 Current state analysis (✅ done)

### Current catalog layout

```text
20260503.02-vision-pe-meta-usecases/
├── README.md                  ← lists 4 folders, uses --dim robustness for adherence
├── usecase-index.json         ← 21 entries across 4 groups
├── 01-freshness/              ← 4 UCs (cross-cutting)
├── 02-quality-gates/          ← 7 UCs (effectiveness — guidance)
├── 03-consumer-correctness/   ← 4 UCs (effectiveness — adherence)
└── 04-efficiency/             ← 8 UCs (efficiency)
```

### Gap

No folder for the **reliability** pole. The catalog has no UC whose subject is process reproducibility, loop stability, multi-pass validation invariant, rollback drill, metadata-guard enforcement, fail-closed boundary, boundary actionability, autonomy calibration, conflict-resolution coverage, portability boundary, or mode-vs-risk decoupling.

## ⚙️ Deliverables

### Change 1: Scaffold `05-reliability/` folder (✅ done)

**Files to create** (use existing UC files as templates — match YAML, section structure, and `Reliability analysis`/`Cost & efficiency`/`Routing` blocks):

| File | Anchor | Priority | Subject |
|---|---|---|---|
| `README.md` | — | — | Folder index, recommended command entry points, run order |
| `p0-01-process-reproducibility.md` | R3 | P0 | Run same command twice on unchanged inputs; diff outputs; non-zero diff = failure |
| `p0-02-regression-protection.md` | R4 | P0 | Enforce before/after behavioral snapshots on every autonomous change; block commit if regression detected |
| `p0-03-metadata-guard-enforcement.md` | R5 + R6 | P0 | Verify pre-change block + post-change reconciliation fires on every change; verify fail-closed on infrastructure staleness |
| `p1-01-loop-stability-audit.md` | R1 | P1 | Scan outcome log for artifact-state oscillation; flag artifacts flipping ≥2 times in N runs |
| `p1-02-multipass-validation-invariant.md` | R2 | P1 | Assert every committed change has ≥1 independent validation pass entry in the outcome log |
| `p1-03-rollback-exercise.md` | R8 | P1 | Tier-scoped snapshot/restore drill; verify rollback succeeds without breaking dependents |
| `p1-04-boundary-actionability-redteam.md` | R7 | P1 | Red-team agent self-constraint enforcement: craft requests matching `scope.excludes` and verify rejection |
| `p2-01-autonomy-calibration-audit.md` | R9 | P2 | Cross-check autonomy gradient classifications against outcome-log success rates; recommend threshold updates |
| `p2-02-conflict-resolution-coverage.md` | R10 | P2 | Verify every conflict pair documented in the rationale-conflict table is exercised by at least one test scenario |
| `p2-03-portability-boundary-scan.md` | R11 | P2 | Scan all PE artifacts for generic (non-`pe-`-prefixed) agent/prompt names that would clash on port |
| `p2-04-mode-vs-risk-decoupling-check.md` | R12 | P2 | Run low-risk autonomous-eligible changes through each command; verify default `--mode plan` never gates them |

**File-content template** (each UC follows this skeleton, mirroring existing UCs):

```markdown
---
id: UC-NN
group: 05-reliability
priority: P0|P1|P2
title: "..."
command_family: "review|update|adherence|scheduled-review"
primary_entry_point: "/pe-meta-... --dim reliability ..."
allowed_option_classes: ["dim","scope","deps","skip","mode"]
---

# <title>

## 🎯 Purpose
## ⚙️ Invocation
## 📋 Process
## 📐 Dimensions covered
## 🚦 Reliability analysis
## 💰 Cost & efficiency
## 🔗 Related use cases
```

### Change 2: Update [`README.md`](../../../../06.00-idea/self-updating-prompt-engineering/20260503.02-vision-pe-meta-usecases/00-overview.md) (✅ done)

| Section | Edit |
|---|---|
| Folder map | Add `05-reliability` row with link to its README, priority flow `P0 → P1 → P2` |
| Quick start | Add "System reliability incident / scheduled reliability audit" trigger → start at `05-reliability` |
| Dimension group shortcuts | Add row: `Reliability \| --dim reliability \| D28–D35 (new) \| Loop stability, reproducibility, rollback, autonomy calibration` |
| Dimension group shortcuts | Rename row: `Consumer correctness \| --dim adherence` (was `--dim robustness`); mark old alias with `(deprecated alias: --dim robustness — redirects with notice for one release)` |
| Complete use-case list | Add `### 05-reliability` section with bullet list of 11 new files |
| Compatibility map | Append rows mapping any anchor previously discussed elsewhere |

### Change 3: Update [`usecase-index.json`](../../../../06.00-idea/self-updating-prompt-engineering/20260503.02-vision-pe-meta-usecases/usecase-index.json) (✅ done)

- Append 11 new entries (UC-23 through UC-33, or next free IDs) under group `05-reliability`.
- Each entry has: `id`, `group`, `priority`, `path`, `order`, `title`, `command_family`, `primary_entry_point`, `allowed_option_classes`.
- Bump `version` to `2.1.0`; update `generated_at`.

### Change 4: Cross-folder consistency sweep (✅ done)

- In `03-consumer-correctness/README.md` and its UCs: replace every occurrence of `--dim robustness` with `--dim adherence`. Add one-line deprecation footnote: *"Old alias `--dim robustness` remains accepted for one release with a deprecation notice."*
- In every UC's "Related use cases" block, add a reliability cross-link where applicable (e.g., `p0-01-dependency-aware-full-review.md` → links to `05-reliability/p0-02-regression-protection.md`).

## 🧪 Validation (✅ done)

- [x] `05-reliability/` exists with README + 11 UC files
- [x] Every R1–R12 anchor in [`overview.md`](overview.md) maps to exactly one P0/P1/P2 UC (verify via a coverage table at the top of `05-reliability/README.md`)
- [x] [`README.md`](../../../../06.00-idea/self-updating-prompt-engineering/20260503.02-vision-pe-meta-usecases/00-overview.md) folder map includes `05-reliability`
- [x] `--dim adherence` appears as the canonical alias in the dimension-group table; `--dim robustness` flagged deprecated
- [x] `usecase-index.json` validates against schema; new entries parseable
- [x] No existing UC is broken by the rename (grep for stale `--dim robustness` references in UC bodies and example invocations)
- [x] Each new UC has a `## 🚦 Reliability analysis` section and a `## 💰 Cost & efficiency` section matching the established UC format

## 📐 Impact assessment

| Factor | Assessment |
|---|---|
| Breaking change? | Minor — `--dim robustness` keeps working with deprecation notice for one release window |
| Version bump | `usecase-index.json` `2.0.0` → `2.1.0` |
| Downstream dependencies | Workstream 3 (pe-meta dimension catalog) — defines the actual D28–D35 dimensions that `--dim reliability` resolves to. This plan can scaffold UCs that *reference* `--dim reliability` even before D-numbers are finalized; UCs MUST be updated when WS3 confirms the dimension IDs. |
| Risk | Low — additive folder; rename has back-compat path |

## 📌 Next steps after this plan

1. Apply Changes 1–4.
2. Coordinate with [01.03 pe-meta update plan](01.03-pe-meta-update-plan.md) on the final D-numbers for the reliability dimensions; update each new UC's "Dimensions covered" block once finalized.
3. Update the parent [`overview.md`](overview.md) Resolution status checklist.
