---
title: "PE-meta normalization: use cases update plan"
author: "Dario Airoldi"
date: "2026-05-23"
categories: [issue, prompt-engineering, pe-meta, use-cases]
description: "Detailed plan for updating PE-meta use cases to align with the normalized command taxonomy, option model, and invocation strategy."
status: "completed"
version: "1.0.0"
parent: "03.01-pe-meta-improvemnt-plan.md"
workstream: "2"
prerequisites: "03.02 (vision update) must be completed first"
---

# PE-meta normalization: use cases update plan

## 🎯 objective (✅ done)

Update the PE-meta use-case catalog so every use case explicitly identifies its command entry point, allowed options, and recommended invocation patterns — conforming to the normalized option model defined in the vision (workstream 1).

## 📋 prerequisites

- **Workstream 1 (vision update) MUST be complete.** This workstream references:
  - Command family definitions
  - Option taxonomy table
  - Preset alias table
  - `--mode` disambiguation decision
  - `--scope` + `--deps` orthogonality principle

## 🔎 current state (✅ done)

The use-case catalog (`20260503.02-vision-pe-meta-usecases/`) contains:
- 4 groups: freshness (4 use cases), quality-gates (7), consumer-correctness (3), efficiency (7)
- Machine-readable index (`usecase-index.json`) with id, group, priority, path, order, title
- Group shortcuts using `--group` syntax (not in canonical option model)
- Quick-start routing table mapping triggers to entry groups
- No explicit command entry points per use case
- No option-class annotations
- No `--scope` or `--skip` usage examples

**Key reconciliation needed:** The README uses `--group freshness` syntax which isn't in the canonical option model. This likely maps to `--dim` groups, but the mapping isn't explicit.

## ⚙️ deliverables (✅ done)

### 1. Reconcile `--group` with `--dim` (✅ done)

**Problem:** Use-case README shows:
```
/pe-meta-update healthcheck --group freshness
/pe-meta-update healthcheck --group quality-gates
```

But the canonical option model has `--dim` (with groups like `structural`, `quality`, `efficiency`, etc.), NOT `--group`.

**Resolution:** Define the mapping between use-case groups and dimension groups:

| Use-case group | Maps to `--dim` | Rationale |
|---|---|---|
| `freshness` | `--dim freshness` (D12, D13) | Staleness + source verification dims |
| `quality-gates` | `--dim quality` (D6-D11, D27) | Guidance quality properties |
| `consumer-correctness` | `--dim robustness` (D5, D6, D16, D18) | Boundaries + adherence + coverage |
| `efficiency` | `--dim optimize` (D3, D7, D9, D11, D20-D26) | Token/efficiency dims |

**Decision needed:** Should `--group` be an alias for `--dim`, or should we replace all `--group` occurrences with `--dim`?

**Acceptance criteria:**
- Every `--group` reference in use-case docs maps to a `--dim` group. (✅ done)
- README updated with canonical syntax. (✅ done)
- If `--group` retained as alias, add to vision alias table. (✅ done — `--group` dropped, replaced with `--dim`)

### 2. Add command entry point annotations to each use case (✅ done)

**Target:** Every use-case markdown file gets a new "Invocation" section (after the trigger/context section).

**Template:**

```markdown
## Invocation

**Command family:** Update (orchestration)
**Primary entry point:** `/pe-meta-update healthcheck --dim freshness`
**Alternative entry points:**
- `/pe-meta-scheduled-review --dim freshness --deps direct` (rotation-triggered, shallow dep check)
- `/pe-meta-release-monitor <url>` (when trigger is external release)

**Supported options:**
| Option | Relevance to this use case |
|---|---|
| `--dim freshness` | Limits to staleness/source dimensions |
| `--scope context` | Focus on context files (most staleness-prone) |
| `--deps direct` | Check immediate consumers of stale artifacts |
| `--skip structure,consistency` | Skip non-relevant audit phases |
```

**Acceptance criteria:**
- All 22 use-case files include an "Invocation" section. (✅ done)
- Every entry point uses canonical option syntax (no deprecated flags). (✅ done)
- Options listed are consistent with the applicability matrix. (✅ done)

### 3. Update `usecase-index.json` metadata (✅ done)

**Add fields to each entry:**

```json
{
  "id": "UC-22",
  "group": "01-freshness",
  "priority": "P0",
  "path": "01-freshness/p0-01-context-quality-lifecycle.md",
  "order": 1,
  "title": "Context quality lifecycle",
  "command_family": "update",
  "primary_entry_point": "/pe-meta-update healthcheck --dim freshness --scope context",
  "allowed_option_classes": ["dim", "scope", "skip", "mode"]
}
```

**Acceptance criteria:**
- Every entry has `command_family`, `primary_entry_point`, `allowed_option_classes`. (✅ done)
- `command_family` values match vision command family names exactly. (✅ done)
- `allowed_option_classes` validated against applicability matrix. (✅ done)

### 4. Route guidance-first use cases to adherence (✅ done)

**Problem:** Quality-gate and consumer-correctness use cases currently suggest review commands for guidance verification. Under the normalized model, guidance-first verification has a canonical command: `/pe-meta-adherence`.

**Affected use cases (to audit):**
- `03-consumer-correctness/p1-01-guidance-adherence-verification.md`
- `03-consumer-correctness/p1-02-model-specific-guidance-adherence.md`
- `02-quality-gates/p2-02-vision-alignment-check.md`

**Action:** For each affected use case:
1. Read current invocation examples
2. Determine if the use case is fundamentally guidance-first (verify adherence to loaded guidance) vs review (assess quality independently)
3. If guidance-first: update primary entry point to `/pe-meta-adherence`
4. If review: keep review entry point, remove guidance-first language

**Acceptance criteria:**
- Guidance-first use cases route to `/pe-meta-adherence` as primary entry point. (✅ done — UC-11, UC-21)
- Review use cases route to `/pe-meta-review` or `/pe-meta-update`. (✅ done — UC-13 kept as review)
- No use case suggests an overloaded or deprecated invocation pattern. (✅ done)

### 5. Add `--skip` stage scenarios (✅ done)

**Add use-case scenarios demonstrating when to skip stages:**

| Scenario | Invocation | Rationale |
|---|---|---|
| No external input available | `--skip research` | Analyze current state only |
| Structure known-good | `--skip structure` | Focus on content quality |
| Cross-artifact consistency verified | `--skip consistency` | Focus on individual artifacts |
| Fast local-only check | `--skip external` | No internet, use local artifacts |
| Combined fast health check | `--skip research,structure` | Minimal viable health check |

**Target:** Add as a new section in the catalog README or as a standalone reference file.

**Acceptance criteria:**
- At least 5 skip scenarios documented with rationale. (✅ done)
- Each scenario includes a complete invocation example. (✅ done)
- Prerequisites for safe skipping documented (e.g., "skip structure only when last structural audit was <7 days ago"). (✅ done)

### 6. Add `--scope` composition scenarios (✅ done)

**Target:** New section in README or standalone file showing scope + deps composition.

**Scenarios to document:**

| Use case | Invocation | Meaning |
|---|---|---|
| Review prompt's context deps | `/pe-meta-prompt-review path --scope context --deps full` | Follow full chain, examine only context files |
| Review agent's instruction adherence | `/pe-meta-agent-review path --scope instructions --deps direct` | Check first-level instruction deps only |
| Audit context file consumers | `/pe-meta-context-review path --scope prompts --deps direct` | Find prompts that load this context |
| Update all context files | `/pe-meta-update --scope context --mode apply` | Iterate context files only in batch |
| Health check prompts only | `/pe-meta-update healthcheck --scope prompts` | Diagnose prompt health, skip other types |

**Acceptance criteria:**
- Both orchestrator and type-specific scope semantics demonstrated. (✅ done)
- Orthogonality with `--deps` shown clearly (scope filters WHAT, deps controls HOW DEEP). (✅ done)
- At least one example per command family that supports `--scope`. (✅ done)

### 7. Update conflict-resolution notes (✅ done)

**Document overlap handling between commands:**

| Overlap | Resolution |
|---|---|
| `release-monitor` vs `update fullcheck <url>` | `release-monitor` is canonical for external-change-driven workflows. `update fullcheck <url>` is compatibility route. |
| `adherence` vs `review --dim robustness` | `adherence` is canonical for "verify against loaded guidance." Review assesses quality independently. |
| `scheduled-review` rotation vs `update healthcheck` | `scheduled-review` orchestrates recurring cadence (auto-rotates `--deps` and dimension selection). `update healthcheck` is ad-hoc invocation. |
| `scheduled-review --deps` vs direct `/pe-meta-adherence` | Every 4th rotation, scheduled-review internally delegates to adherence. For ad-hoc adherence checks, invoke `/pe-meta-adherence` directly. |

**Acceptance criteria:**
- Every command-overlap pair has explicit "which to use when" guidance. (✅ done)
- No circular routing (command A says "use B", command B says "use A"). (✅ done)

### 8. Update existing use cases with `--scope` in recommended invocations (✅ done)

**Audit all 21 use cases and add `--scope` where it improves precision:**

- Freshness use cases → likely benefit from `--scope context` (context files are most staleness-prone)
- Efficiency use cases → likely benefit from `--scope agents,prompts` (token budgets most relevant there)
- Consumer-correctness → `--scope` less relevant (already target-specific)

**Acceptance criteria:**
- Each use case's "recommended invocation" reviewed against scope opportunity. (✅ done)
- Where `--scope` adds precision, it's included with rationale. (✅ done)
- Where `--scope all` is correct, it's left implicit (default). (✅ done)

## 🧪 validation (✅ done)

### Consistency checks

1. **Cross-reference integrity**: every `command_family` in `usecase-index.json` matches a family name from the vision. (✅ done)
2. **Option validity**: every option used in use-case invocation examples is valid for its command per the applicability matrix. (✅ done)
3. **No deprecated syntax**: no `--group`, `--with-deps`, `--with-deps-shallow`, `--no-external`, `--no-research`, `--skip-*` in any use-case example (only canonical forms or documented aliases). (✅ done)
4. **Completeness**: every use case has a primary entry point and at least one invocation example. (✅ done)

### User-path validation

For each use-case group, trace the user path:
1. User has trigger (e.g., "platform release noticed")
2. Quick-start table routes to a group
3. Group README shows command entry point
4. Use case shows full invocation with options
5. Invocation is valid per applicability matrix

**Acceptance criteria:** All 4 group paths trace cleanly without ambiguity. (✅ done)

## 📋 exit criteria (✅ done)

1. All 22 use cases have "Invocation" section with command entry point and options. (✅ done)
2. `usecase-index.json` extended with command family and option class metadata. (✅ done)
3. `--group` → `--dim` reconciliation complete. (✅ done)
4. Guidance-first use cases routed to `/pe-meta-adherence`. (✅ done)
5. `--skip` and `--scope` scenarios documented. (✅ done)
6. Conflict-resolution table complete. (✅ done)
7. Validation checks pass (no deprecated syntax, all options valid). (✅ done)

## 🔗 dependencies

- **Upstream:** Workstream 1 (vision) — command families, option taxonomy, preset aliases, `--mode` disambiguation
- **Downstream:** Workstream 3 (implementation) — use-case invocation examples become the test cases for prompt parser validation

## 📚 references

- `03.01-pe-meta-improvemnt-plan.md` (master plan)
- `03.02-pe-meta-improvemnt-visionupdate-plan.md` (prerequisite: vision update)
- `06.00-idea/self-updating-prompt-engineering/20260503.02-vision-pe-meta-usecases/README.md`
- `06.00-idea/self-updating-prompt-engineering/20260503.02-vision-pe-meta-usecases/usecase-index.json`
- `.github/prompts/00.09-pe-meta/pe-meta-adherence.prompt.md`

<!--
validations:
  grammar: {status: "not_run", last_run: null}
  readability: {status: "not_run", last_run: null}
  structure: {status: "not_run", last_run: null}

article_metadata:
  filename: "03.03-pe-meta-improvemnt-usecasesupdate-plan.md"
-->
