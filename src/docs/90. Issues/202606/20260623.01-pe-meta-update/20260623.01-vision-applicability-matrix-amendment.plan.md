---
title: "Plan: Reconcile the vision § Option applicability matrix — flip the Creation row to parity and remove the obsolete Update column (v15.10.0 amendment)"
author: "Dario Airoldi"
date: "2026-06-24"
categories: [plan, prompt-engineering, vision, governance, vision-amendment, staleness-review]
description: "Vision-amendment plan that proposes the reconciling edits for PL-1-vision-matrix (the last park-lot item of the PE meta design/review improvement plan). The vision's § Option applicability matrix Creation row marks `--mode`, `--dim`, `--skip`, `--plan-file` as ❌, contradicting the vision's own § Plan output contract and the design/review parity contract; the matrix also still carries a post-v15.9 Update column. This plan documents the exact old→new edits to flip the four Creation cells to ✅ (keeping `--deps ❌`, genuinely inapplicable to a not-yet-existing artifact), remove the Update column, reconcile three dependent prose blocks, and bump the vision v15.9.0 → v15.10.0. The vision is human-only; this plan is the proposal the vision owner applies. Downstream artifacts (the implementation `pe-meta-option-applicability-matrix.md`, `pe-meta-design.prompt.md`, `pe-meta-review.prompt.md`) were already reconciled toward parity in the parent improvement plan, so every item here is `[vision-only: no-downstream]`."
draft: false
status: "done"
last_updated: "2026-06-24"
severity: "Medium"
component: "[20260531.01-vision.md](../../../../../06.00-idea/self-updating-prompt-engineering/20260531.01-vision.md) § Option applicability matrix + dependent prose"
framework: "GitHub Copilot Customization (PE-meta vision v15.9 → v15.10)"
---

# Plan — Vision § Option applicability matrix reconciliation (v15.10.0 amendment)

**Parent plan:** [01-pe-meta-design-review-improvement-plan.md](01-pe-meta-design-review-improvement-plan.md) — park-lot item **PL-1-vision-matrix**
**Plan ID:** `20260623.01-vision-applicability-matrix-amendment`
**Date:** 2026-06-24
**Status:** Done — all proposed edits (Edits A–E) were applied to the vision on 2026-06-24 with the vision owner's explicit authorization. The vision is now at v15.10.0; the actionability gate passed (see [§ Actionability gate](#-actionability-gate--self-check)).

---

## 🎯 Goal

> **Original trigger (verbatim):** "**PL-1-vision-matrix — Vision § Option applicability matrix reconciliation (human-only).** The vision's Creation row (`--dim ❌`, `--mode ❌`, `--plan-file ❌`) contradicts the vision's own Plan-output contract and design/review parity contract, and still lists a post-v15.9 **Update** column. Reconciling it is a vision edit. → `20260623.01-vision-applicability-matrix-amendment.plan.md` (will spawn; vision-amendment plan)."

Reconcile the § Option applicability matrix in [20260531.01-vision.md](../../../../../06.00-idea/self-updating-prompt-engineering/20260531.01-vision.md) so it stops contradicting two of the vision's own contracts and stops carrying a family that v15.9.0 already retired:

1. **§ Plan output contract** (P2): "A plan file is the single pivot artifact every mutating run passes through" and "Plan file MUST be written on every mutating run." Creation is a mutating ("Write — new artifact") run, so it MUST support `--mode` (plan/apply) and `--plan-file` — yet the matrix marks both ❌ for Creation.
2. **Design/review parity** (scope item `design-review-parity`, P1; ratified as decision **D1-parity-authority** in the parent plan): design accepts the same option surface as review except where genuinely artifact-type-inapplicable. `--dim` and `--skip` are not artifact-type-inapplicable to Creation, yet the matrix marks them ❌.
3. **v15.9.0 consolidation:** Update was folded into Review (`pe-meta-update` → deprecation stub redirecting to `pe-meta-review`), and the P1 `command-families` enumeration was narrowed to Creation / Review / Guidance-first / Scheduled — but the matrix still carries a stale **Update** column and three prose mentions of an "Update" family.

Every goal item below carries a per-item scope tag, principle-impact attestation, and downstream landing per [vision-amendment.instructions.md](../../../../../.github/instructions/vision-amendment.instructions.md).

| # | Item | Scope tag | Principle impact | Downstream landing |
|---|---|---|---|---|
| 1 | Flip the matrix **Creation** row cells `--mode`, `--dim`, `--skip`, `--plan-file` from ❌ to ✅ (keep `--deps ❌`); update the § Default-full semantics `--mode` row to mark Creation applicable (plan/apply, default `apply`) | `[vision-only: no-downstream]` | preserves: `command-family-agnostic` (P0), `minimal-canonical-surface` (P0), `default-full-invocation` (P1); touches: `design-review-parity` (P1 justification: realizes the parity contract in the matrix — Creation accepts the same option surface as Review except `--deps`, genuinely inapplicable to a not-yet-existing artifact with no dependency graph) | `landing: vision-only` |
| 2 | Remove the obsolete **Update** column from the matrix and reconcile its three dependent prose blocks (Key design decisions bullets; § Default-full semantics `--mode` row; § Default-full semantics `--deps` row) | `[vision-only: no-downstream]` | preserves: `command-family-agnostic` (P0), `minimal-canonical-surface` (P0); touches: `command-families` (P1 justification: matrix + prose catch-up to the v15.9.0 enumeration that already folded Update→Review; no family is added or removed) | `landing: vision-only` |
| 3 | Bump the vision v15.9.0 → v15.10.0 (title token + frontmatter) and add a "Most recent changes (v15.10.0)" entry recording the reconciliation, observing the frontmatter size cap in [vision-frontmatter.instructions.md](../../../../../.github/instructions/vision-frontmatter.instructions.md) | `[vision-only: no-downstream]` | preserves: `minimal-canonical-surface` (P0), `metadata-guarded-changes` (P1); touches: none — version-governance bookkeeping required by the vision's own change protocol | `landing: vision-only` |

**Why all three items are `[vision-only: no-downstream]`.** The downstream artifacts that derive from this matrix — the implementation [pe-meta-option-applicability-matrix.md](../../../../../.github/prompts/00.09-pe-meta/pe-meta-option-applicability-matrix.md) and the [pe-meta-design.prompt.md](../../../../../.github/prompts/00.09-pe-meta/pe-meta-design.prompt.md) / [pe-meta-review.prompt.md](../../../../../.github/prompts/00.09-pe-meta/pe-meta-review.prompt.md) prompts — were already reconciled toward parity in the parent improvement plan (workstreams WS-I/WS-II, now `done`). This amendment closes the lagging normative vision text only; there is no further downstream artifact to update, hence `landing: vision-only`.

**Version impact & consent.** No P0 principle is weakened: flipping the Creation cells makes the option surface *more* uniform across families, which strengthens — not touches — `command-family-agnostic` (P0); the canonical eight-parameter surface is unchanged, preserving `minimal-canonical-surface` (P0). Because no P0 is classified as `touches:`, no P0 consent line is mandated by [vision-amendment.instructions.md](../../../../../.github/instructions/vision-amendment.instructions.md). A vision version bump is still proposed (item 3) because the matrix is normative vision text and v15.9.0 established the convention that every matrix/contract change bumps the version and adds a "Most recent changes" entry. Human consent to proceed with the reconciliation recorded: **Dario Airoldi, 2026-06-24**.

---

## 📋 Table of contents

- [🎯 Goal](#-goal)
- [📋 Exit criteria](#-exit-criteria)
- [⚙️ Actionability gate — self-check](#-actionability-gate--self-check)
- [✏️ Proposed vision edits](#-proposed-vision-edits)
- [🅿️ Park lot](#-park-lot)
- [🔓 Open decisions](#-open-decisions)
- [⚠️ Boundaries and risks](#-boundaries-and-risks)
- [📚 References](#-references)

---

## 📋 Exit criteria

- **Matrix Creation row reconciled** in the vision: `--mode ✅`, `--dim ✅`, `--skip ✅`, `--plan-file ✅`, `--deps ❌` (unchanged), `--scope ✅`, `--source ✅`, `--start`/`--end ✅` (unchanged). (✅ done)
- **Update column removed** from the matrix; the matrix header and every body row carry exactly four family columns (Creation / Review / Guidance-first / Scheduled). (✅ done)
- **Three dependent prose blocks reconciled**: the "Key design decisions" bullets carry no live "Update" family; the § Default-full semantics `--mode` row names Creation applicable and drops the Update mention; the `--deps` row drops "Update" from its family list. (✅ done)
- **Vision version bumped** v15.9.0 → v15.10.0 with a "Most recent changes (v15.10.0)" entry, observing the frontmatter size cap. (✅ done)
- **No P0 principle weakened**: `command-family-agnostic` and `minimal-canonical-surface` remain intact; the change-log entry states this explicitly. (✅ done)
- **Parent park-lot item closed**: [PL-1-vision-matrix](01-pe-meta-design-review-improvement-plan.md) is resolved now that the vision owner has applied these edits. (✅ done)

---

## ⚙️ Actionability gate — self-check

Run against the 8-check gate in [plan-execution.instructions.md](../../../../../.github/instructions/plan-execution.instructions.md) (v1.4.0). Check 8 (principle impact) applies because this is a vision-amendment plan.

| # | Check | Result on this plan |
|---|---|---|
| 1 | **Goal alignment** — § Goal opens with verbatim trigger restatement; every item realizes it | ✅ Pass — PL-1 text quoted verbatim; items 1–3 each realize one facet of the matrix reconciliation |
| 2 | **Goal reachability** — the items together reach the goal | ✅ Pass — flip + column removal + prose reconciliation + version bump fully reconcile the matrix |
| 3 | **Execution determinism** — each edit has an exact old→new | ✅ Pass — [§ Proposed vision edits](#-proposed-vision-edits) gives literal before/after for every block |
| 4 | **Clarity & actionability** — no bare verify/check/assess verbs | ✅ Pass — every item is a concrete flip/remove/add edit, not a verification step |
| 5 | **Unknown resolution** — no blocking unknown remains | ✅ Pass — the `--deps` judgment (keep ❌) and the full-parity direction are settled and approved |
| 6 | **Scope discipline** — every item tagged; zero `[scope-expansion]` in the active list | ✅ Pass — all three items are `[vision-only: no-downstream]`; the residual-Update audit is parked |
| 7 | **Coverage promise** — every item names a landing | ✅ Pass — all items are `[vision-only]` with `landing: vision-only` (exempt; downstream pre-reconciled) |
| 8 | **Principle impact** — preserves/touches listed; P0 touches consented; P1 touches justified | ✅ Pass — every item lists `preserves:`; the two P1 touches (`design-review-parity`, `command-families`) carry inline justifications; no P0 is touched |

**Verdict:** ✅ All 8 checks pass. Plan was `status: actionable`; the edits were then applied to the vision (human-authorized) and the plan moved to `status: done`.

---

## ✏️ Proposed vision edits

The edits below are literal old→new blocks against [20260531.01-vision.md](../../../../../06.00-idea/self-updating-prompt-engineering/20260531.01-vision.md). The vision is **human-only** — these are proposals for the vision owner to apply (or to explicitly authorize the assistant to apply). They are grouped by location; goal-item attribution is noted on each.

### Edit A — § Option applicability matrix (items 1 + 2)

Find the intro line and table under `#### Option applicability matrix`. Replace the table.

**Old:**

```text
| Parameter | Creation | Review | Guidance-first | Scheduled | Update |
|---|---|---|---|---|---|
| `--mode` | ❌ | ✅ | ✅ | ❌ | ✅ |
| `--scope` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `--source` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `--dim` | ❌ | ✅ | ✅ | ✅ | ✅ |
| `--start` / `--end` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `--deps` | ❌ | ✅ | ✅ | ✅ | ✅ |
| `--skip` | ❌ | ✅ | ✅ | ✅ | ✅ |
| `--plan-file` | ❌ | ✅ | ✅ | ❌ | ✅ |
```

**New:**

```text
| Parameter | Creation | Review | Guidance-first | Scheduled |
|---|---|---|---|---|
| `--mode` | ✅ | ✅ | ✅ | ❌ |
| `--scope` | ✅ | ✅ | ✅ | ✅ |
| `--source` | ✅ | ✅ | ✅ | ✅ |
| `--dim` | ✅ | ✅ | ✅ | ✅ |
| `--start` / `--end` | ✅ | ✅ | ✅ | ✅ |
| `--deps` | ❌ | ✅ | ✅ | ✅ |
| `--skip` | ✅ | ✅ | ✅ | ✅ |
| `--plan-file` | ✅ | ✅ | ✅ | ❌ |
```

Net cell changes (Creation column): `--mode ❌→✅`, `--dim ❌→✅`, `--skip ❌→✅`, `--plan-file ❌→✅`; `--deps` stays ❌; the entire **Update** column is removed.

### Edit B — "Key design decisions" bullets (item 2, with item 1's Creation note)

**Old:**

```text
- **`--mode` is NOT applicable to Scheduled**: Scheduled review uses `--deps` for traversal depth control and an internal rotation mechanism for mode cycling. See § Mode elimination from scheduled review.
- **`--mode` IS applicable to Update**: Update is write-permitted, but `--mode plan` lets the caller preview the proposed changes as an actionable plan file before committing. The default when omitted is `apply` (see Default-full semantics by parameter); `--mode plan` is the opt-in preview.
- **External platform reconciliation is an Update invocation**: To reconcile artifacts against an external platform change, invoke `/pe-meta-update --source <url>` (optionally with `--start`/`--end` to bound the source window). There is no separate release-diff family — the Update family with an external `--source` covers it.
- **`--start` / `--end` applies to every family**: The window may scope research (Update / Review / Scheduled), external source-diff selection (Update with an external `--source`), or seed-corpus selection (Creation, Guidance-first). A bound may be a date or a source-version token; a version-shaped bound REQUIRES a singleton `--source` (see § Option taxonomy → value-shape parser).
```

**New:**

```text
- **`--mode` is NOT applicable to Scheduled**: Scheduled review uses `--deps` for traversal depth control and an internal rotation mechanism for mode cycling. See § Mode elimination from scheduled review.
- **`--mode` IS applicable to Creation**: Creation is write-permitted (new artifact), but `--mode plan` lets the caller preview the proposed creation as an actionable plan file before committing. The default when omitted is `apply` (see Default-full semantics by parameter); `--mode plan` is the opt-in preview. This realizes the § Plan output contract — every mutating run, Creation included, passes through one plan artifact.
- **External platform reconciliation is a Review invocation**: To reconcile artifacts against an external platform change, invoke `/pe-meta-review --source <url>` (optionally with `--start`/`--end` to bound the source window). There is no separate release-diff or Update family — Review with an external `--source` covers it (Update was consolidated into Review in v15.9.0).
- **`--start` / `--end` applies to every family**: The window may scope research (Review / Scheduled), external source-diff selection (Review with an external `--source`), or seed-corpus selection (Creation, Guidance-first). A bound may be a date or a source-version token; a version-shaped bound REQUIRES a singleton `--source` (see § Option taxonomy → value-shape parser).
```

### Edit C — § Default-full semantics by parameter, `--mode` row (items 1 + 2)

**Old:**

```text
| `--mode` | `apply` (where applicable) | Findings + changes. Assessment-only requires explicit `--mode plan`. Not applicable to Creation, Scheduled (see applicability matrix); applicable to Update with `apply` default. |
```

**New:**

```text
| `--mode` | `apply` (where applicable) | Findings + changes. Assessment-only requires explicit `--mode plan`. Applicable to Creation, Review, and Guidance-first with `apply` default; not applicable to Scheduled (see applicability matrix). |
```

### Edit D — § Default-full semantics by parameter, `--deps` row (item 2)

**Old:**

```text
| `--deps` | Command-specific natural depth | Review / Update / Scheduled / Guidance-first: `direct`. Creation: not applicable. |
```

**New:**

```text
| `--deps` | Command-specific natural depth | Review / Scheduled / Guidance-first: `direct`. Creation: not applicable. |
```

### Edit E — Version bump + "Most recent changes" entry (item 3)

1. **Title token:** change the vision title's version token `(v15.9)` → `(v15.10)`; update the frontmatter version field that mirrors it.
2. **Change-log entry:** prepend a new entry at the top of the "Most recent changes" block (≈ line 290), trimming/rotating the oldest entry if needed to honor the size cap in [vision-frontmatter.instructions.md](../../../../../.github/instructions/vision-frontmatter.instructions.md):

```text
**Most recent changes (v15.10.0, 2026-06-24)** — § Option applicability matrix reconciled to parity + Update column removed. Flipped the Creation row to `--mode ✅`, `--dim ✅`, `--skip ✅`, `--plan-file ✅` (keeping `--deps ❌`, genuinely inapplicable to a not-yet-existing artifact with no dependency graph), so the matrix conforms to the § Plan output contract (every mutating run, Creation included, passes through one plan artifact) and the design/review parity contract (D1-parity-authority). Removed the obsolete **Update** column and its three dependent prose mentions, completing the v15.9.0 Update→Review consolidation. No P0 principle weakened: `command-family-agnostic` and `minimal-canonical-surface` are preserved/strengthened (the option surface is now uniform across families; the canonical eight-parameter surface is unchanged). Touches P1 `design-review-parity` (realized) and the P1 `command-families` enumeration (matrix catch-up only).
```

---

## 🅿️ Park lot

- **Residual live-"Update" reference audit (vision-wide).** Beyond the § Option applicability matrix and the three dependent prose blocks scoped here, the vision body may carry other mentions of a live "Update" family that v15.9.0's architectural consolidation did not sweep. Auditing the whole vision is outside PL-1's matrix scope. → **defer** (open a follow-up staleness pass if a reader finds residual references; not blocking this amendment).

---

## 🔓 Open decisions

_None._ The two decisions that gated this plan are resolved:

- **Full-parity vs minimal flip** → full parity (flip `--mode`, `--dim`, `--skip`, `--plan-file`), approved by the vision owner.
- **`--deps` for Creation** → keep ❌ (genuine inapplicability: a not-yet-existing artifact has no dependency graph to traverse), approved.

---

## ⚠️ Boundaries and risks

- **The vision is human-only.** Per the vision frontmatter boundary ("MUST NOT be modified by autonomous processes — vision changes are human-only"), this plan does **not** auto-apply the edits. It is the proposal + execution-ready specification; the vision owner applies [§ Proposed vision edits](#-proposed-vision-edits) (or explicitly authorizes the assistant to apply them on their behalf).
- **No downstream cascade.** Downstream artifacts were reconciled ahead of the vision in the parent improvement plan, so applying these edits closes the gap without triggering further changes. If a future reader finds a downstream artifact still keyed to the old Creation row, that is a separate staleness finding.
- **Frontmatter size cap.** Edit E must honor the "Most recent changes" block size cap; the vision owner may need to rotate the oldest entry out when prepending the v15.10.0 entry.
- **Reversibility.** All edits are git-tracked and trivially revertible; the change-log entry makes the change auditable and attributable.

---

## 📚 References

- **📒** [01-pe-meta-design-review-improvement-plan.md](01-pe-meta-design-review-improvement-plan.md) — parent plan; PL-1-vision-matrix park-lot item and decision D1-parity-authority
- **📘** [vision-amendment.instructions.md](../../../../../.github/instructions/vision-amendment.instructions.md) — per-item scope tag + principle impact + coverage promise rules this plan is shaped to
- **📘** [plan-execution.instructions.md](../../../../../.github/instructions/plan-execution.instructions.md) — 8-check actionability gate and lifecycle
- **📘** [vision-frontmatter.instructions.md](../../../../../.github/instructions/vision-frontmatter.instructions.md) — `principles:` block + "Most recent changes" size cap audited here
- **📕** [20260531.01-vision.md](../../../../../06.00-idea/self-updating-prompt-engineering/20260531.01-vision.md) — the human-only amendment target (§ Option applicability matrix, § Default-full semantics, § Plan output contract)
- **📒** [01-vision-usecase-plan-rules-plan.md](../../202605/20260525.03-staleness-review/05-vision-usecase-plan-rules/01-vision-usecase-plan-rules-plan.md) — canonical worked example for vision-amendment plan shape
