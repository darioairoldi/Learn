Alignment rule (H14, validation rule HIGH): every YAML boundary MUST have a corresponding Always/Never entry, and no body entry may contradict YAML.


Runtime-grounding rule: the ## 🚨 CRITICAL BOUNDARIES body section is the enforcement mechanism for the YAML boundaries: — but it must reference them operationally, "it doesn't restate the full [list]… This is NOT duplication. YAML remains the single source of truth; the body is the enforcement mechanism."

3. Are the body boundaries redundant with the YAML boundaries:? — Yes, and incomplete (the worse problem)
